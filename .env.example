DISCORD_TOKEN=your-bot-token-here
DATABASE_PATH=gamebot.db
# Optional: your server's ID (enable Developer Mode in Discord settings,
# then right-click the server icon -> Copy Server ID). When set, slash
# commands sync to just this server in seconds instead of up to an hour
# for a global sync -- recommended for a single-server bot like this one.
GUILD_ID=
